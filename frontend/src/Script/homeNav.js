const nav = document.getElementsByClassName('nav');

        window.addEventListener('scroll', () => {
            if (window.scrollY >= 200) {
              document.getElementById("nav").style.background = "inherit";
             
            } else{
              document.getElementById("nav").style.background = "inherit";
            }
        })